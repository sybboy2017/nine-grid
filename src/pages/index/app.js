import { App } from '@fes/mkv-frame'; // 固定
import shell from './shell'; // 固定，引入shell

/*
非首屏组件、methods、线上数据
*/
// eslint-disable-next-line no-unused-vars
const app = new App({
    shell,
	components: {
	},
    methods: {
        init() {
        },
    },
    created() {
    },
});
